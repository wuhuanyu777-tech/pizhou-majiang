- var page = 'rule'

doctype html
html(lang="zh-CN")

  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")

    include ../inc/ogp
    +ogp('邳州麻将: 规则设置', 'カスタマイズしたルールを対局に使用できます')

    include ../inc/style
    include ../inc/script

  body.rule
    include ../inc/loaderr
    include ../inc/navi
    .version ver. #{version}

    include ../inc/rule
