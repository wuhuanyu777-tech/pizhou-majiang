/*
 *  Majiang.rule  (邳州麻将·查胡麻将版)
 */
"use strict";

module.exports = function(param = {}) {

    let rule = {
        /* 结算相关 */
        '配給原点': 0,                      // 起始分数（筹码制从0开始）
        '順位点':   ['0','0','0','0'],      // 不按名次加分，直接按总分数排名

        /* 邳州麻将计分 */
        '底胡':   10,                       // 胡牌者固定 +10 胡
        '胡底分': 1,                        // 1胡 = 1分
        '幺底分': 10,                       // 1幺 = 10分
        '荤底':   30,                       // 飘荤时每家额外 +30 分
        '包庄あり':  true,
        '找拧あり':  false,                  // 找拧交互较复杂，默认关闭

        /* 赤牌/宝牌（邳州麻将无） */
        '赤牌': { m: 0, p: 0, s: 0 },
        'クイタンあり': true,
        '喰い替え許可レベル': 0,

        /* 局数相关 */
        '場数':             1,              // 1: 东风战（4局一庄）
        '途中流局あり':     true,           // 起手同牌流局
        '流し満貫あり':     false,
        'ノーテン宣言あり': false,
        'ノーテン罰あり':   false,
        '最大同時和了数': 2,               // 一炮多响
        '連荘方式':         1,              // 庄家胡牌连庄（由 game.js 逻辑处理）
        'トビ終了あり':     false,
        'オーラス止めあり': false,
        '延長戦方式':       0,

        /* 日麻要素全部关闭 */
        '一発あり':         false,
        '裏ドラあり':       false,
        'カンドラあり':     false,
        'カン裏あり':       false,
        'カンドラ後乗せ':   false,
        'ツモ番なしリーチあり':   false,
        'リーチ後暗槓許可レベル': 0,

        /* 役満相关（无） */
        '役満の複合あり':   false,
        'ダブル役満あり':   false,
        '数え役満あり':     false,
        '役満パオあり':     false,
        '切り上げ満貫あり': false,
    };

    for (let key of Object.keys(param)) {
        rule[key] = param[key];
    }

    return rule;
}
